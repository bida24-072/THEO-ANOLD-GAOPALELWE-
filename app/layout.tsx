import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Theo Gaopalelwe | BIDA Portfolio",
  description: "Business Intelligence & Data Analytics student at Botswana Accountancy College. Python, SQL, Data Analytics, Web Development, Cloud Fundamentals.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
